# Archived native applications

TokenTracker's active product is the local web dashboard served by the loopback CLI backend. Native macOS and Windows applications are unsupported and no longer built, tested, versioned, or released.

This directory retains historical native workflows, tests, and release helpers for reference only. They are not active authorities and must not be re-enabled without an explicit product-scope decision and new acceptance criteria.
